import 'package:flutter/material.dart';

class SizeModel
{
  bool? isSelected;
  String? size;

  SizeModel(this.size,this.isSelected);

}


class ColorModel
{
  bool? isSelected;
  Color? color;

  ColorModel(this.color,this.isSelected);


}